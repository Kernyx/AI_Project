# Runtime Data

This directory is created for local runtime files:

- `biometric.db` - SQLite database
- `faiss.index` - FAISS vector index
- `faiss_meta.json` - FAISS id counter metadata

These files are generated by the application and must not be committed.

