# Рабочие данные

В этой папке приложение создает локальные файлы:

- `biometric.db` - SQLite-база
- `faiss.index` - FAISS-индекс
- `faiss_meta.json` - метаданные счетчика FAISS id

Эти файлы генерируются приложением и не должны попадать в git.
